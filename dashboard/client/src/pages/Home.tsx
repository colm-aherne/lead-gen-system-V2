import DashboardLayout from "@/components/DashboardLayout";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { cn } from "@/lib/utils";
import { trpc } from "@/lib/trpc";
import { CheckCircle2, ExternalLink, Loader2, Mail, MapPin, Phone, Search, ShieldCheck, XCircle } from "lucide-react";
import { useMemo, useState } from "react";
import { toast } from "sonner";

type Status = "new" | "verified" | "drafted" | "moved" | "opted_out";
type Lead = {
  id: number; businessName: string; businessType: string; address: string; phone: string; website: string;
  status: Status; optedOut: boolean; duplicateOutreach: boolean;
  tracker: { id: number; emailDraft: string; emailSent: boolean; responseReceived: boolean } | null;
};

const statusStyle: Record<Status, string> = {
  new: "bg-blue-50 text-blue-700 border-blue-200", verified: "bg-amber-50 text-amber-800 border-amber-200",
  drafted: "bg-violet-50 text-violet-800 border-violet-200", moved: "bg-slate-100 text-slate-700 border-slate-200",
  opted_out: "bg-rose-50 text-rose-700 border-rose-200",
};
const formatStatus = (status: Status) => status.replace("_", " ");

export default function Home() {
  const [search, setSearch] = useState("");
  const [status, setStatus] = useState<Status | "all">("all");
  const [selectedId, setSelectedId] = useState<number | null>(null);
  const query = trpc.leads.list.useQuery({ search: search || undefined, status: status === "all" ? undefined : status });
  const utils = trpc.useUtils();
  const optOut = trpc.leads.optOut.useMutation({
    onSuccess: async () => { await utils.leads.list.invalidate(); toast.success("Lead marked as opted out."); },
    onError: error => toast.error(error.message),
  });
  const contacted = trpc.leads.markContacted.useMutation({
    onSuccess: async () => { await utils.leads.list.invalidate(); toast.success("Contact history saved; lead removed from the outreach queue."); },
    onError: error => toast.error(error.message),
  });
  const selected = useMemo(() => (query.data?.find(lead => lead.id === selectedId) ?? query.data?.[0] ?? null) as Lead | null, [query.data, selectedId]);

  return <DashboardLayout><div className="mx-auto w-full max-w-[1520px] space-y-5 pb-8">
    <header className="flex flex-col gap-4 border-b border-slate-200 pb-5 md:flex-row md:items-end md:justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-slate-500">Nationwide Ireland</p><h1 className="mt-1 text-2xl font-semibold tracking-tight text-slate-950">Trade & service leads</h1><p className="mt-1 text-sm text-slate-600">Review contact methods, outreach history, drafts, and lead status in one place.</p></div><div className="flex items-center gap-2 text-sm text-slate-600"><ShieldCheck className="h-4 w-4 text-emerald-600" />Tracker entries are protected from duplicate outreach</div></header>
    <section className="grid gap-4 xl:grid-cols-[minmax(0,1fr)_390px]"><div className="min-w-0 space-y-3"><div className="flex flex-col gap-2 sm:flex-row"><div className="relative flex-1"><Search className="pointer-events-none absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-slate-400" /><Input value={search} onChange={event => setSearch(event.target.value)} placeholder="Search by business, type, city, or status" className="h-10 border-slate-300 pl-9 shadow-none" /></div><Select value={status} onValueChange={value => setStatus(value as Status | "all")}><SelectTrigger className="h-10 w-full border-slate-300 shadow-none sm:w-[170px]"><SelectValue placeholder="All statuses" /></SelectTrigger><SelectContent><SelectItem value="all">All statuses</SelectItem>{(["new", "verified", "drafted", "moved", "opted_out"] as Status[]).map(item => <SelectItem value={item} key={item}>{formatStatus(item)}</SelectItem>)}</SelectContent></Select></div>{selected ? <div className="rounded-lg border border-slate-200 bg-white p-5 xl:hidden"><LeadDetail lead={selected} busy={optOut.isPending || contacted.isPending} onOptOut={() => optOut.mutate({ leadId: selected.id })} onContacted={() => contacted.mutate({ leadId: selected.id })} /></div> : null}
      <div className="overflow-hidden rounded-lg border border-slate-200 bg-white"><div className="overflow-x-auto"><table className="w-full min-w-[860px] text-left text-sm"><thead className="border-b border-slate-200 bg-slate-50 text-xs font-semibold uppercase tracking-wide text-slate-500"><tr><th className="px-4 py-3">Business</th><th className="px-4 py-3">Type</th><th className="px-4 py-3">Location</th><th className="px-4 py-3">Phone</th><th className="px-4 py-3">Website</th><th className="px-4 py-3">Status</th></tr></thead><tbody className="divide-y divide-slate-100">{query.isLoading ? <tr><td colSpan={6} className="px-4 py-12 text-center text-slate-500"><Loader2 className="mr-2 inline h-4 w-4 animate-spin" />Loading leads…</td></tr> : null}{query.isError ? <tr><td colSpan={6} className="px-4 py-12 text-center text-rose-700">Unable to load leads: {query.error.message}</td></tr> : null}{!query.isLoading && !query.isError && !query.data?.length ? <tr><td colSpan={6} className="px-4 py-12 text-center text-slate-500">No leads match the current filters.</td></tr> : null}{query.data?.map(lead => <tr key={lead.id} onClick={() => setSelectedId(lead.id)} className={cn("cursor-pointer transition-colors hover:bg-slate-50", selected?.id === lead.id && "bg-sky-50/70")}><td className="max-w-[210px] px-4 py-3 font-medium text-slate-950"><div className="truncate">{lead.businessName}</div>{lead.duplicateOutreach ? <span className="mt-1 inline-flex items-center gap-1 text-xs text-violet-700"><ShieldCheck className="h-3 w-3" />Already tracked</span> : null}</td><td className="px-4 py-3 text-slate-700">{lead.businessType}</td><td className="max-w-[190px] px-4 py-3 text-slate-700"><div className="line-clamp-2">{lead.address}</div></td><td className="px-4 py-3 text-slate-700">{lead.phone}</td><td className="px-4 py-3">{lead.website ? <a href={lead.website} target="_blank" rel="noreferrer" onClick={event => event.stopPropagation()} className="inline-flex items-center gap-1 text-sky-700 hover:underline"><ExternalLink className="h-3.5 w-3.5" />Open</a> : <span className="text-slate-400">—</span>}</td><td className="px-4 py-3"><Badge variant="outline" className={cn("capitalize", statusStyle[lead.status])}>{formatStatus(lead.status)}</Badge></td></tr>)}</tbody></table></div><div className="border-t border-slate-100 px-4 py-2 text-xs text-slate-500">Showing up to 500 matching leads. Select any row to inspect stored contact methods.</div></div></div>
      <aside className="hidden min-h-[460px] rounded-lg border border-slate-200 bg-white p-5 xl:sticky xl:top-4 xl:block xl:h-[calc(100vh-3rem)] xl:overflow-y-auto">{selected ? <LeadDetail lead={selected} busy={optOut.isPending || contacted.isPending} onOptOut={() => optOut.mutate({ leadId: selected.id })} onContacted={() => contacted.mutate({ leadId: selected.id })} /> : <div className="flex h-full flex-col items-center justify-center text-center text-sm text-slate-500"><Mail className="mb-3 h-7 w-7 text-slate-300" />Select a lead to view its contact details and outreach history.</div>}</aside></section>
  </div></DashboardLayout>;
}

function LeadDetail({ lead, busy, onOptOut, onContacted }: { lead: Lead; busy: boolean; onOptOut: () => void; onContacted: () => void }) {
  const cannotContact = lead.duplicateOutreach || lead.status === "opted_out";
  return <div className="space-y-5"><div className="border-b border-slate-200 pb-4"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.14em] text-slate-500">Lead #{lead.id}</p><h2 className="mt-1 text-lg font-semibold text-slate-950">{lead.businessName}</h2><p className="mt-1 text-sm text-slate-600">{lead.businessType}</p></div><Badge variant="outline" className={cn("capitalize", statusStyle[lead.status])}>{formatStatus(lead.status)}</Badge></div>{lead.duplicateOutreach ? <div className="mt-3 flex gap-2 rounded-md bg-violet-50 p-3 text-sm text-violet-900"><ShieldCheck className="mt-0.5 h-4 w-4 shrink-0" />This business already has an outreach-tracker record. Direct contact is disabled to prevent duplicate outreach.</div> : null}</div><section><h3 className="text-xs font-semibold uppercase tracking-[0.14em] text-slate-500">Contact methods</h3><div className="mt-3 space-y-3 text-sm"><div className="flex gap-3"><Phone className="mt-0.5 h-4 w-4 text-slate-400" /><a href={lead.phone === "No phone recorded" ? undefined : `tel:${lead.phone}`} className={cn("break-all", lead.phone === "No phone recorded" ? "text-slate-500" : "text-sky-700 hover:underline")}>{lead.phone}</a></div><div className="flex gap-3"><ExternalLink className="mt-0.5 h-4 w-4 text-slate-400" />{lead.website ? <a href={lead.website} target="_blank" rel="noreferrer" className="break-all text-sky-700 hover:underline">{lead.website}</a> : <span className="text-slate-500">No website recorded</span>}</div><div className="flex gap-3"><MapPin className="mt-0.5 h-4 w-4 text-slate-400" /><a target="_blank" rel="noreferrer" href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(lead.address)}`} className="text-sky-700 hover:underline">{lead.address}</a></div></div></section><section><h3 className="text-xs font-semibold uppercase tracking-[0.14em] text-slate-500">AI draft & outreach history</h3>{lead.tracker ? <div className="mt-3 space-y-3 rounded-md border border-violet-200 bg-violet-50/50 p-3"><div className="flex items-center gap-2 text-sm font-medium text-violet-900"><Mail className="h-4 w-4" />Tracker entry #{lead.tracker.id}</div>{lead.tracker.emailDraft ? <p className="whitespace-pre-wrap text-sm leading-6 text-slate-700">{lead.tracker.emailDraft}</p> : <p className="text-sm text-slate-600">No draft text was stored. This tracker row may record direct contact.</p>}<div className="text-xs text-slate-600">{lead.tracker.emailSent ? "Marked as contacted" : "Draft saved for review"}{lead.tracker.responseReceived ? " · Response recorded" : ""}</div></div> : <div className="mt-3 rounded-md border border-slate-200 bg-slate-50 p-3 text-sm text-slate-600">No outreach-tracker entry exists. This lead has not been drafted or recorded as contacted.</div>}</section><section className="border-t border-slate-200 pt-4"><h3 className="text-xs font-semibold uppercase tracking-[0.14em] text-slate-500">Lead actions</h3><div className="mt-3 grid gap-2"><Button variant="outline" disabled={busy || lead.status === "opted_out"} onClick={onOptOut} className="justify-start border-rose-200 text-rose-700 hover:bg-rose-50 hover:text-rose-800"><XCircle className="mr-2 h-4 w-4" />Mark opted out</Button><Button disabled={busy || cannotContact} onClick={onContacted} className="justify-start bg-slate-900 text-white hover:bg-slate-800"><CheckCircle2 className="mr-2 h-4 w-4" />Mark contacted directly</Button>{cannotContact ? <p className="text-xs text-slate-500">Contacting is available only when no tracker entry exists and the lead has not opted out.</p> : null}</div></section></div>;
}
